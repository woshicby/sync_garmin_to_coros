# archive/ — 归档工具

一次性 / 调试用的历史工具，**保留留档但不建议继续使用**。

## 文件说明

| 文件 | 原用途 | 为何归档 |
|------|--------|---------|
| `rename_fit_files.py` | 批量修正 FIT 文件名的日期时间为文件内部记录的开始时间 | 时间提取逻辑已统一到 `core/fit_parser.py`，下载流程已内置该功能 |
| `improved_fit_parser.py` | 调试用 FIT 解析（手工解析时间戳候选） | 已被 `garmin_fit_sdk` + `core/fit_parser.py` 取代 |

## 说明

- 归档文件不再维护，import 已更新为 `core.config`（保证可导入），但逻辑未改动
- 如果发现需要重新使用其中的功能，建议先在 `core/` 中实现/扩展，而不是直接运行归档脚本
- 删除归档文件前请确认对应功能已完全被 `core/` 覆盖
